This project automates file backup to an AWS S3 bucket using Python and CI/CD pipelines.
The backup process is scheduled using GitHub Actions cron triggers, enabling fully automated cloud-based backups without requiring a dedicated server.
